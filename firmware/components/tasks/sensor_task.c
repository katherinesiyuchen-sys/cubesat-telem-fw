#include "sensor_task.h"

#include "esp_log.h"

static const char *TAG = "sensor_task";

void sensor_task_start(void) {
    ESP_LOGI(TAG, "sensor task placeholder ready; GNSS telemetry is active through lora_task");
}
